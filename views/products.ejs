<!-- products.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Products</title>
</head>
<body>
  <h1>Ürünler</h1>
  <ul>
    <% products.forEach(product => { %>
      <li>
        <h2><%= product.name %></h2>
        <p><%= product.description %></p>
        <p>Fiyat: <%= product.price %> TL</p>
      </li>
    <% }); %>
  </ul>
</body>
</html>
